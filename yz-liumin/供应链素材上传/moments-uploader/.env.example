PORT=3100
SESSION_SECRET=replace-with-a-long-random-string
MG_CENTER_BASE_URL=https://mg-cli.meimeifa.com

# Local development only. Do not enable in production.
DEV_LOGIN_BYPASS=0

# Production should keep this disabled so the tool only enters when full creation flow is available.
ALLOW_PREVIEW_ONLY=0

# Optional fallback only. The normal full flow uses the logged-in user's SCM authorization.
SCM_BEARER_TOKEN=
SCM_BUSINESS_TOKEN=
SCM_ACCOUNT_ID=715
SCM_ACCOUNT_NAME=技术部刘敏
